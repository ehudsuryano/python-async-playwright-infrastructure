[pytest]
#addopts = --maxfail=3 --strict-markers -v --html=report.html --self-contained-html
asyncio_mode = auto
reruns = 0
reruns-delay = 2
markers =
    smoke: smoke tests
    regression: regression tests